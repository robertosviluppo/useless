dist: xenial

os: linux

language: node_js

node_js:
  - node
  - lts/*
  - 14
  - 13
  - 12
  - 11
  - 10

install:
  - npm install

script:
  - echo $TRAVIS_NODE_VERSION
  - npm run cover

after_script: "cat coverage/lcov.info | node_modules/coveralls/bin/coveralls.js"
